<%# Update follow button after successful unfollow action %>
<%= turbo_stream.replace "follow_button_#{@followed.id}" do %>
  <%= render 'users/follow_button', user: @followed %>
<% end %>

<%# Optional: Show a notification %>
<%= turbo_stream.prepend "notifications" do %>
  <div class="alert alert-info alert-dismissible fade show" role="alert">
    Ya no sigues a <%= @followed.user_name %>
    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
  </div>
<% end %>
